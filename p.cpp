#include <stdio.h>
#include <stdlib.h>

int main(){

    int datos[f][c];
    int f,c;

    for(f=0; f<=2; f++)
	for(c=0; c<5; c++){
	    printf("clase e id: ");
    	    scanf("%d %d", datos[f][c]);
        }

    return EXIT_SUCCESS;
}
